<div>Hello World</div>
    <div>
        {% for todo in fasta %}
            <div class="task">
                <small>
                    <a href="/download_fasta?fasta={{ todo }}">{{ todo }}</a>
                    {%  if todo in nj_tree %}
                    <a href="/download_nj_tree?fasta={{ todo }}">{{ todo }}</a>
                    {% endif %}
                </small>
            </div>
        {% endfor %}
    </div>

